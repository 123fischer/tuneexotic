<div class="show">
    <div class="container">
    <div class="card">
      <h1><%= @tune.name %></h1>
      <%= @tune.description %>
      <% if current_user %>
        <%= @tune.url %>
      <% end %>
      <%= simple_form_for [@tune, @request] do |f| %>

      <%= f.submit "Add request", class: "btn btn-primary"  %>
    <% end %>

    </div>
  </div>
</div>
